#ifndef STRING_UTILITY_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define STRING_UTILITY_H

#include <string>
#include <vector>
#include <iostream>
#include <sstream>
using namespace std;

vector<string> split_string(string);

string str_toupper(string);

#endif