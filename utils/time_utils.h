#ifndef TIME_UTILS_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define TIME_UTILS_H

#include <iostream>
#include <chrono>

using namespace std;

uint64_t get_current_time();

#endif

